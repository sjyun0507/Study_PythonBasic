# 외부 모듈의 이용

# 1. 패키지
# 모듈의 상위 개념, 모듈의 집합을 의미
# ----> 모듈이 모여있는 디렉터리

# 파이썬에서 기본적으로 내장되어 있지 않더라도 외부에서 만들어진 패키지를 다운하면 패키지에 포함된 모듈을 사용할 수 있다.

# 2. 패키지 관리자
# pip 라는 이름의 패키지 관리자를 이용해서 패키지를 추가하거나 삭제할 수 있다.

# 3. 패키지 설치/삭제 (cmd(컴퓨터 실행창)에서 사용)
# 형식
# pip install/uninstall 패키지명
# ex) pip install numpy,  pip uninstall numpy

# PyCharm - settings - project - python interpreter에서 수동으로 설치/삭제 가능

# import numpy
# 설치 후에는 내장 모듈을 사용하는것과 동일하게 사용 할 수 있다.
